# Grave Below — freestanding OpenGL voxel game

`Grave Below` is an adventure game. You **MUST** explore to survive!

`git clone --recurse-submodules git@github.com:setbe/grave-below.git`

`git submodule update --init --recursive --remote`